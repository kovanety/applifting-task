import React, { FC } from 'react'
import { RouteComponentProps } from '@reach/router'

import { Layout } from '../components/Layout'

export const HomePage: FC<RouteComponentProps> = () => {
  return <Layout>Ahoj</Layout>
}
